---
apiVersion: v1
kind: Service
metadata:
  name: fdk-harvest-admin-service
spec:
  selector:
    app: fdk-harvest-admin-service
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: NodePort
